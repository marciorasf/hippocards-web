import { makeStyles } from "@material-ui/core"

const useStyles = makeStyles((_theme) => ({
  titleFont: {
    fontSize: "1.125rem",
    fontWeight: 700,
  },
  paragraph: {
    whiteSpace: "pre",
  },
}))

export default useStyles
