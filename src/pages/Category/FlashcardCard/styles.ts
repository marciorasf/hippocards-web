import { makeStyles } from "@material-ui/core"

const useStyles = makeStyles((_theme) => ({
  card: {
    height: "100%",
  },
  questionFont: {
    fontSize: "1.125rem",
    fontWeight: 700,
  },
}))

export default useStyles
